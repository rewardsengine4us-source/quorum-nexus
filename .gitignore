node_modules
.next
out
.env
.env.local
.env.production
.env*.local
.DS_Store
*.log
*.tsbuildinfo
.vercel

# Superseded by supabase/functions/live-sync/ (see commit "move live sync to
# a Supabase background task"). These import puppeteer-core, which is no
# longer a dependency; listed here because this working tree cannot delete
# them and an accidental re-add would break the build.
lib/browserless.ts
lib/liveSyncDriver.ts
app/api/live-sync/probe/
